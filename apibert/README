README
------

This directory contains the RESTful API used to access the data brisbert uses. 
To run the api (see the bottom of the script for details on port and address) , 
simply run:

python apibert

Assumes python2 is installed as the default python.